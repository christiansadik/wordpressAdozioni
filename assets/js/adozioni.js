/**
 * jQuery starts
 */
jQuery( document ).ready(function($) {

    // Redirect page after click
    $(document).on('click', '.post-68252', function (){
      $(window.location).attr('href', 'http://www.aclodv.org/volontariato/');
    });
    
  });